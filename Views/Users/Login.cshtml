
@{
    ViewData["Title"] = "Create";
}

<h1>Login</h1>

<h4>Login</h4>
<hr />
<div class="row">
    <div>
        <form asp-action="Login" method="post">
            
            <div class="form-group">
                <label  class="control-label">Kullanıcı adı ya da mail giriniz.</label>
                <input for="userName" name="userName" class="form-control" />
                
            </div>
            <div class="form-group">
                <label  class="control-label"> Şifre giriniz.</label>
                <input for="password" name="password" class="form-control" />
                
            </div>
            
            <div class="form-group">
                <input type="submit" value="Login" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }